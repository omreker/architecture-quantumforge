# Задание 1. Исследование моделей и инфраструктуры

## 1. Сравнение LLM

| Критерий | Локальные (Hugging Face) | Облачные OpenAI | YandexGPT |
| --- | --- | --- | --- |
| Качество ответов | Хорошее на 8B-14B. Использование качественного поиска в RAG-системе позволяет выдать точность ответов на уровне 80-90% от показателей топовых моделей. | Высокое (94% accuracy в RAG-бенчмарках, низкий hallucination). | Высокое. В RAG чуть ниже Claude / GPT. |
| Скорость работы | 20-80 токенов/с на GPU, 5-15 токенов/с на CPU. | 50-150+ токенов/с, низкий TTFT. | Сопоставимо с OpenAI в Yandex Cloud. |
| Стоимость владения и использования | На железо и электричество. | Стоимость зависит от объёма используемых токенов. | Аналогично, зависит от объёма токенов. |
| Удобсвто и простота развёртывания | Средняя сложность. | Максимально простое (API). | Простое (Yandex Cloud AI Studio, OpenAI-совместимый API). |

*Вывод:* для QuantumForge (чувствительные данные SCADA/SOC 2) предпочтительнее локальные модели или YandexGPT.

## 2. Сравнение моделей эмбеддингов

| Критерий | Локальные (Sentence-Transformers) | Облачные (OpenAI Embeddings) |
| --- | --- | --- |
| Скорость создания индекса | 5-25 мс/документ на CPU, ещё быстрее на GPU. | 30-80 мс + сетевая задержка. |
| Качество поиска | bge / e5 / gte-base близки к OpenAI small. | Лучшее на английском. |
| Стоимость владения и использования | 0 (после скачивания модели ~100-500 МБ). | $0.02 / 1M токенов (small), $0.13 (large). |

*Вывод:* предпочтительнее локальные модели (`sentence-transformers/all-MiniLM-L6-v2` для скорости или `BAAI/bge-base-en-v1.5` / `multilingual-e5-base` для качества).

## 3. Сравнение векторных БД: ChromaDB vs FAISS

| Критерий | FAISS | ChromaDB |
| --- | --- | --- |
| Скорость поиска и индексации | Очень высокая (0.3-1.5 мс). | Хорошая (2-8 мс), чуть медленнее. |
| Сложность внедрения и поддержки | Средняя (ручное управление index + metadata). | Низкая (embedded, простой API). |
| Удобство в работе | Библиотека, нужно писать обёртки. | Готовая vector DB, REST, metadata filters. |
| Стоимость владения | 0 + RAM/диск. | 0 + RAM/диск (SQLite + файлы). |

*Вывод:* для прототипного RAG-бота предпочтителен FAISS (быстрее, проще в Docker). ChromaDB лучше для быстрых прототипов с фильтрами.

## 4. Рекомендуемая конфигурация сервера

**Минимальный (прототип / 50-100 одновременных пользователей):**

- CPU: 8 vCPU
- RAM: 32 ГБ
- GPU: Нет (или RTX 4060 8 ГБ для локальной LLM 7-8B Q4)
- Диск: 100 ГБ SSD

**Рекомендуемый (prodcution, локальная LLM + FAISS):**

- CPU: 16 vCPU
- RAM: 64-128 ГБ
- GPU: RTX 4090 (24 ГБ) для LLM 13-32B + embeddings
- Диск: 500 ГБ NVMe

**Облачный вариант (Yandex Cloud):**

- Managed Kubernetes + GPU node pool
- Отдельный сервис для embeddings + FAISS / Qdrant
- LLM через YandexGPT API или self-hosted vLLM

## 5. Итоговые рекомендации

### Вариант 1

- LLM: Локальная через LangChain + mock / простой transformers (или OpenAI если есть ключ)
- Embeddings: all-MiniLM-L6-v2
- Vector DB: FAISS
- Интерфейс: FastAPI + Docker Compose
- Плюсы: Нулевая стоимость API, полная конфиденциальность, простота деплоя.
- Минусы: Качество ответов ниже frontier-моделей.

### Вариант 2

- LLM: YandexGPT 5
- Embeddings: multilingual-e5-large
- Vector DB: FAISS или Qdrant
- Плюсы: Отличный русский язык, хорошее качество.
- Минусы: Зависимость от облака.

### Вариант 3

- LLM: GPT-4o / Claude Sonnet
- Embeddings: text-embedding-3-large
- Vector DB: Pinecone / Qdrant Cloud
- Плюсы: Лучшие метрики RAG.
- Минусы: Стоимость + утечка данных.

### Вариант 4

- LLM: Qwen3-32B / Llama-3.1-70B (vLLM)
- Embeddings: bge-m3 или fine-tuned
- Vector DB: Qdrant
- Плюсы: Полный контроль, масштабируемость, соответствие аудитам.
- Минусы: Высокая стоимость железа и DevOps.

**Рекомендация для QuantumForge:** начать с *Варианта 1*, затем перейти на *Вариант 2* или *Вариант 4* в зависимости от бюджета и требований безопасности. FAISS + локальные embeddings полностью закрывают требования к конфиденциальности и стоимости на старте.
